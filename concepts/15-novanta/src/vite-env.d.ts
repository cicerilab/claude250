/// <reference types="vite/client" />

/*
 * Tipi di Vite per gli import con suffisso: `?raw` (markup SVG delle icone,
 * assets/svg/index.ts) e `?url` (foto .webp, assets/foto/index.ts). Nessun
 * plugin: sono nativi di Vite.
 */
