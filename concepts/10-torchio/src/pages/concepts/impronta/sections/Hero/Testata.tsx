/**
 * STUB dello scaffold-engineer: la testata (marchio, tre voci, "Prova la
 * tua") è del section-builder-hero (tech-architect §4), che riscrive questo
 * file per intero. Impronta.tsx la monta PRIMA del <main>, come fratello del
 * contenuto: così `position: sticky` vale per tutta la pagina.
 * Zona da lasciare libera: il bottone "Torna in Ciceri Lab" del sito
 * (in alto a sinistra ~230×44 px su desktop, in basso a sinistra sotto 640 px).
 */
import './testata.css';

export default function Testata() {
  return <header className="imp-testata" />;
}
